# self-check report (regenerated by `make check`)
*_check_report.txt

# maintainer/owner tooling — kept locally, never shipped to participating teams
maintainer/

# R / editor / OS cruft
.Rhistory
.RData
.Rproj.user/
.DS_Store

# Python cruft
__pycache__/
*.pyc

# simulation working outputs (pilot/probe runs); final deposit artifacts are
# placed explicitly in raw_data_deposit/ during packaging (Block 5/6)
pipeline/sim_out/
pipeline/sim_probe/
